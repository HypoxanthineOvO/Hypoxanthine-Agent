from __future__ import annotations

from hypo_agent.core.markdown_plaintext import render_markdown_plaintext


def render_qq_plaintext(markdown_text: str) -> str:
    return render_markdown_plaintext(markdown_text)
