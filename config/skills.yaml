default_timeout_seconds: 30
skills:
  tmux:
    enabled: true
    timeout_seconds: 30
  code_run:
    enabled: true
    timeout_seconds: 30
  filesystem:
    enabled: true
  reminder:
    enabled: true
