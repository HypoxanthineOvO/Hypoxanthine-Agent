# Locked Python dependencies for deployment/dev bootstrap.
# Note: install the local package separately (see README): `pip install -e .`

fastapi==0.128.7
uvicorn[standard]==0.40.0
pydantic==2.12.5
PyYAML==6.0.3
structlog==25.5.0
APScheduler==3.11.2
litellm==1.82.0
aiosqlite==0.22.1
PyMuPDF==1.27.1
python-pptx==1.0.2
python-docx==1.2.0
websockets==16.0

# Test tooling (repo expects `pytest -q` to work out of the box)
pytest==8.4.2
pytest-cov==5.0.0

